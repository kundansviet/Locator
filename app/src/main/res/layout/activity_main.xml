<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.aw.locationtracker.MainActivity">
    <android.support.v4.view.ViewPager
        android:id="@+id/pager_home"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/rl_bottom_layout">
    </android.support.v4.view.ViewPager>

    <RelativeLayout
        android:id="@+id/rl_bottom_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true">

        <android.support.design.widget.TabLayout
            android:id="@+id/tab_home"
            android:background="@android:color/white"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="1dp"
            android:scrollIndicators="bottom"
            app:tabTextColor="#929292"
            app:tabSelectedTextColor="@color/cyan_text"
            app:tabIndicatorColor="@color/cyan_text"
            app:tabGravity="fill"
            app:tabPaddingStart="0dp"
            app:tabPaddingEnd="0dp"
            app:tabTextAppearance="@style/CustomTabWidgetText"
            app:tabIndicatorHeight="2dp"
            app:tabMode="fixed">

        </android.support.design.widget.TabLayout>
    </RelativeLayout>

</RelativeLayout>
